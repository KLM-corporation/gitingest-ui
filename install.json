{
  "run": [
    {
      "method": "shell.run",
      "params": {
        "message": [
          "if not exist build\\Release mkdir build\\Release",
          "tar -xf bin\\GitingestUI-Windows.zip -C build\\Release"
        ]
      }
    },
    {
      "method": "notify",
      "params": {
        "html": "Installation finished. Click 'start' to run GitingestUI."
      }
    }
  ]
}
